import React, { FC } from 'react';
import UnifiedEntry from './components/UnifiedEntry/UnifiedEntry';

const App: FC = () => {
        return (
            <UnifiedEntry />
    );
}
export default App;

